export type User = {
    createAt?: string;
    email: string;
    id: string;
    login: string;
    name: string;
    surname: string;
    password?: string;
    pictureUrl: string
    _id?: string;
}